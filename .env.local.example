# Clé API OpenAI — requise pour la génération des programmes
OPENAI_API_KEY=sk-votre-cle-ici

# Modèle OpenAI (optionnel, défaut: gpt-4o-mini — rapide, adapté à Vercel)
# Mettre gpt-4o pour plus de qualité (nécessite Vercel Pro pour éviter les timeouts)
OPENAI_MODEL=gpt-4o-mini
